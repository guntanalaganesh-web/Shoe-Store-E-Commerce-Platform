<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' | Shoe Store' : 'Shoe Store' %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="container">
            <nav class="nav">
                <a href="/" class="nav-logo">
                    👟 <span>Shoe</span>Store
                </a>
                
                <ul class="nav-links">
                    <li><a href="/shop">Shop All</a></li>
                    <li><a href="/shop?gender=Men">Men</a></li>
                    <li><a href="/shop?gender=Women">Women</a></li>
                    <li><a href="/shop?category=Running">Running</a></li>
                    <li><a href="/shop?category=Casual">Casual</a></li>
                </ul>
                
                <div class="nav-actions">
                    <form action="/search" method="GET" class="search-form">
                        <input type="text" name="q" id="search-input" placeholder="Search..." class="search-input">
                    </form>
                    
                    <% if (typeof user !== 'undefined' && user) { %>
                        <a href="/auth/profile" class="nav-user">👤 <%= user.firstName %></a>
                        <% if (user.role === 'admin') { %>
                            <a href="/admin" class="btn btn-sm btn-secondary">Admin</a>
                        <% } %>
                        <a href="/auth/logout" class="btn btn-sm btn-outline">Logout</a>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-sm btn-outline">Login</a>
                        <a href="/auth/register" class="btn btn-sm btn-primary">Sign Up</a>
                    <% } %>
                    
                    <a href="/cart" class="nav-cart">
                        🛒
                        <span class="cart-count" style="display: <%= (typeof cart !== 'undefined' && cart.items && cart.items.length > 0) ? 'flex' : 'none' %>">
                            <%= typeof cart !== 'undefined' && cart.items ? cart.items.reduce((sum, item) => sum + item.quantity, 0) : 0 %>
                        </span>
                    </a>
                </div>
            </nav>
        </div>
    </header>

    <% if (typeof success !== 'undefined' && success) { %>
        <div class="alert alert-success container"><%= success %></div>
    <% } %>
    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-error container"><%= error %></div>
    <% } %>

    <main>
